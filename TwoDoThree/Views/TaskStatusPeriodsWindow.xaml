<Window x:Class="TwoDoThree.Views.TaskStatusPeriodsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Status Periods"
        Icon="../Assets/AppIcon.ico"
        Height="420"
        Width="760"
        MinHeight="320"
        MinWidth="620"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Task.Title}"
                   FontSize="16"
                   FontWeight="SemiBold"
                   TextTrimming="CharacterEllipsis"
                   Margin="0,0,0,12" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Periods}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Status"
                                    Width="140"
                                    Binding="{Binding StatusDisplay}" />
                <DataGridTextColumn Header="Start Time"
                                    Width="170"
                                    Binding="{Binding StartTime, StringFormat={}{0:g}}" />
                <DataGridTextColumn Header="End Time"
                                    Width="170"
                                    Binding="{Binding EndTimeDisplay}" />
                <DataGridTextColumn Header="Hours"
                                    Width="90"
                                    Binding="{Binding Hours, StringFormat={}{0:N4}}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Width="84"
                    Content="Close"
                    Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
